import styled from "styled-components";
import { color, layout, space, flexbox, typography } from "styled-system";

const Box = styled("div")(color, layout, space, flexbox, typography);

export default Box;
